<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fanjin.word__bar">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".showlx"></activity>
        <activity android:name=".QueryAct"></activity>
        <activity android:name=".TellAct"></activity>
        <activity android:name=".showabout"></activity>
        <activity android:name=".books"></activity>
        <activity android:name=".trash"></activity>
        <activity android:name=".showWord"></activity>
        <activity android:name=".wordMenu"></activity>
        <activity android:name=".wordInList"></activity>
        <activity android:name=".result"></activity>
    </application>

</manifest>